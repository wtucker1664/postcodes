# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - Asserts
        - \Helper\Unit
        - Symfony:
            app_path: 'app'
            environment: 'test'
    step_decorators: ~        